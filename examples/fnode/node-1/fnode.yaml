name: node-1

features:
  s::runtime.type: node
  app: true
  cli:
    mcp:
      enabled: true

commands:
  cli-test: 
    - fnode build
    - fnode cli

  app-test: 
    - fnode build
    - fnode app

  both-test:
    - fork:
        - fnode cli
        - fnode app
    - pause: "Press any key to continue..."