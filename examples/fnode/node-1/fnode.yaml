name: node-1

features:
  s::runtime.type: node

commands:
  both:
    - fork:
        - fnode cli
        - fnode app
    - pause: "Press any key to continue..."

  hello:
    - echo "Hello from node-1 project!"

  test:
    - node -e "console.log('Testing node-1 project...')"
