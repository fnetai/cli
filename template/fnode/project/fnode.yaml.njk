name: {{name}}

{% if runtime === 'node' %}
features:
  s::runtime.type: node
{% elif runtime === 'python' %}
features:
  s::runtime.type: python
{% elif runtime === 'bun' %}
features:
  s::runtime.type: bun
{% endif %}