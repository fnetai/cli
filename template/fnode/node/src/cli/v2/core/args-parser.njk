{# ============================================================================
   ARGUMENT PARSER
   ============================================================================
   Parses command-line arguments and extracts CLI mode
   ============================================================================ #}

const parseArgs = () => {
  return argsParser(process.argv.slice(2));
};

